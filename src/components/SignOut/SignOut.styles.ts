import styled from "@emotion/styled";

export const StyledSignout = styled("button")({
  fontSize: "1.2rem",
  "&:hover": {
    textDecoration: "underline",
  },
  color: "lightgrey",
  background: "none",
  cursor: "pointer",
});
